.wrapper
  = render partial: 'posts/header'
  .main-content
    %h2 #{@user.name}の記事一覧
    - @posts.each do |post|
      .main-content__article
        = link_to "■【#{post.title}】", post_path(post), class: 'main-content__article__detail'
  %ul
    %li=paginate(@posts)
  .footer